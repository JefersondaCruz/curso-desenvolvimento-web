<?php

    // A definição do contador
    $x = 0;

    // Inicio / definição da estrutura
    while ($x < 10){

        // Corpo do loop
        echo $x . "\n";

        // Incremento do contador
        $x++;
    }   

    echo "Continuando código \n";

       // A definição do contador
       $y = 0;

       // Inicio / definição da estrutura
       while ($y <= 10){
   
           // Corpo do loop
           echo $y . "\n";
   
           // Incremento do contador
           $y += 2;

       }  



        // A definição do contador
        $z = 10;

        // Inicio / definição da estrutura
        while ($z > 0){
    
            // Corpo do loop
            echo $z . "\n";
    
            // Incremento do contador
            $z--;
 
        }  


        // A definição do contador
        $a = 10;

        // Inicio / definição da estrutura
        while ($a > 0){
    
            // Corpo do loop
            if ($a % 2 != 0) {
                echo $a . "\n";
            }
    
            // Incremento do contador
            $a--;
 
        }  